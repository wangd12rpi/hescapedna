_target_: hescape.data_modules.dnameth_wsi_dataset.DnaMethWSIDataModule
root_dir: ${paths.data_root_dir}/train # Paths in JSONL are relative to train/
jsonl_path: ${paths.data_root_dir}/train/index_tile.jsonl
#batch_size: 1
num_workers: 4
pin_memory: true
persistent_workers: true
val_ratio: 0.1
test_ratio: 0.0
seed: 42
min_site_presence: 0.0  # Keep sites present in at least this fraction
max_sites: null  # No limit on CpG sites
verbose: true
