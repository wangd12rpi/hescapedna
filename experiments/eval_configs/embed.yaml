# Stage 1: embed
cache:
  dir: ${project_root:}/experiments/evaluation/cache

dataset:
  cpgpt_vocab_path: ${project_root:}/data/cpgpt_files/dependencies/model/vocab/cancer.json
  processed_beta_dir: ${project_root:}/eval_cache
  dropna: true
  train:
    root_dir: ${project_root:}/data/wsi_dnameth/train
    index_path: ${dataset.train.root_dir}/index_tile.jsonl
  test:
    root_dir: ${project_root:}/data/wsi_dnameth/test
    index_path: ${dataset.test.root_dir}/index_tile.jsonl

clip:
  checkpoint_path: ${project_root:}/results/human-lung-healthy-panel_20251028_132155/local/checkpoints/last.ckpt
  # NEW: exact Lightning hparams.yaml to reconstruct the model. No assumptions.
  hparams_path: ${project_root:}/results/human-lung-healthy-panel_20251028_132155/local/csv_logger/version_0/hparams.yaml
  device: null
  batch_size: 8
  normalize_output: true

# Which embedders to compute
embedders: ["clip_gigapath"]
